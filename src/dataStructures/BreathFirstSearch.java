package dataStructures;

//is usually a prefered methed, searches one layer of depth at a time
// Use some sort of VISITED FLAG to not go inf
// uses a queue, example -> add first node children to list -3> pull first queue node
// -> is final destination? No? add visited flag and add node's children to queue -> repeat from 3

public class BreathFirstSearch {
}
